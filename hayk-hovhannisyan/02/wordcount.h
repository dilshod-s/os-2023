#ifndef WORDCOUNT_H
#define WORDCOUNT_H

// Function to count the number of lines in a file given a file descriptor (fd)
int countLines(int fd);

// Function to count the number of words in a file given a file descriptor (fd)
int countWords(int fd);

#endif // WORDCOUNT_H
